services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nebolla
      POSTGRES_PASSWORD: nebolla
      POSTGRES_DB: nebolla
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes: { pgdata: {} }
